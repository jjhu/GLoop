import { RemoveIonIconSvgStrokeDirective } from './remove-ion-icon-svg-stroke.directive';

describe('RemoveIonIconSvgStrokeDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveIonIconSvgStrokeDirective();
    expect(directive).toBeTruthy();
  });
});
